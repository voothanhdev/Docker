#!/bin/bash
version="$MAGERUN_VERSION"

if  [ ! -f /usr/local/bin/m2 ]; then
    if [ -n "${version}" ]; then
        version="-${version}"
    fi

    curl -o m2 "https://files.magerun.net/n98-magerun2${version}.phar";
    chmod +x m2
    mv m2 /usr/local/bin/
fi

m2 "$@"